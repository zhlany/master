syntax = "proto3";

package grpc_demo;
option go_package = "./pb";

message Teacher {
  int32 id = 1 [(gogoproto.jsontag)="ID,omtempty"];
  string name = 2;
}

service SayName{
  rpc SayHello(Teacher) returns (Teacher);
}